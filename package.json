{
  "name": "rapide-monorepo",
  "private": true,
  "engines": {
    "node": ">=12",
    "pnpm": ">=6"
  },
  "scripts": {
    "format": "prettier --write 'packages/**/*.ts'"
  },
  "devDependencies": {
    "prettier": "^2.3.2"
  },
  "prettier": {
    "tabWidth": 4,
    "semi": false,
    "singleQuote": true,
    "arrowParens": "avoid"
  }
}